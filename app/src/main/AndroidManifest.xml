<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunkai.lab.androidlab">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".diyview.DIYViewActivity" />
        <activity android:name=".diybanner.DIYBannerActivity" />
        <activity android:name=".mixtypeset.MixTextImageActivity" />
        <activity android:name=".diyloading.DiyLoadingActivity" />
        <activity android:name=".patheffect.PathEffectActivity" />
        <activity
            android:name=".diyscroller.DIYSliderActivity"
            android:theme="@style/Theme.TranslucentActionBar.Overlay" />
        <activity android:name=".timeline.TimeLineActivity" />
        <activity android:name=".miclock.MiClockActivity" />
        <activity android:name=".sensor.SensorActivity" />
        <activity android:name=".fish.FishActivity"></activity>
    </application>

</manifest>